{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run CMake Target",
            "type": "cppdbg", // use cppdbg on linux
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${workspaceFolder}/build"
        }
    ]
}
